/**
 * @param {string} A
 * @param {string} B
 * @return {boolean}
 */
var buddyStrings = function(A, B) {
    let arrA = A.split('')
    let arrB = B.split('')
    for (let i = 0; i < arrA.length; i ++) {
        
    }
};

console.log(buddyStrings('ab', 'ba'))